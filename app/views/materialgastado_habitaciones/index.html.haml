<h1>Material gastado por habitacion</h1>
%table.table.table-striped
  %thead
    %tr
      %td='ID'
      %td='Num de material por habitación'
      %td='Cantidad'
      %td='Num de material'
      %td='Num de Servicio de limpieza por habitación'
      %td='Acciones'

  %tbody
    - @materialgh.each do |ma|
      %tr
        %td=ma.id
        %td=ma.idMaterialH
        %td=ma.Cantidad
        %td=ma.idMaterial
        %td=ma.idServicioLH
        %td
          = link_to edit_materialgastado_habitacione_path(ma) do
            = fa_icon 'pencil'
          = link_to materialgastado_habitacione_path(ma), method: :delete, data: {confirm: '¿Estás seguro?'} do
            = fa_icon 'trash'
