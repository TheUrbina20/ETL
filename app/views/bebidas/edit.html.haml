= simple_form_for @bebida, url:  bebida_path, method: :put do |f|
    = f.input :id
    = f.input :nombre
    = f.input :precio
    = f.input :descripcion
    = f.button :submit, class: 'btn btn-primary'
