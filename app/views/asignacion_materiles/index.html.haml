%table
  %thead
    %tr
      %td='id'
      %td='idAsignacion'
      %td='Cantidad'
      %td='idHabitacion'
      %td='idMaterial'
      %td='Acción'

  %tbody
    - @asignacionm.each do |ma|
      %tr
        %td=ma.id
        %td=ma.idAsignacion
        %td=ma.Cantidad
        %td=ma.idHabitacion
        %td=ma.idMaterial
        %td
          = link_to edit_asignacion_materile_path(ma) do
            = fa_icon 'pencil'
          = link_to asignacion_materile_path(ma), method: :delete, data: {confirm: '¿Estás seguro?'} do
            = fa_icon 'trash'
