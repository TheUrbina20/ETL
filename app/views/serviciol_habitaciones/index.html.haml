<h1>Servicio de limpieza por habitacion</h1>
%table.table.table-striped
  %thead
    %tr
      %td='ID'
      %td='Num de servicio de limpieza por habitación'
      %td='Fecha'
      %td='Num de servicio de limpieza'
      %td='Num de habitación'
      %td='Num de empleado'
      %td='Acciones'
  %tbody
    - @serviciolh.each do |s|
      %tr
        %td=s.id
        %td=s.idServicioLH
        %td=s.Fecha
        %td=s.idServicioL
        %td=s.idHabitacion
        %td=s.idEmpleado
        %td
          = link_to edit_serviciol_habitacione_path(s) do
            = fa_icon 'pencil'
          = link_to serviciol_habitacione_path(s), method: :delete, data: {confirm: '¿Estás seguro?'} do
            = fa_icon 'trash'
