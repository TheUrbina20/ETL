= simple_form_for @servicio_habitacion, url:  servicio_habitacione_path, method: :put do |f|
    = f.input :id
    = f.input :id_habitacion_rentada
    = f.input :id_renta
    = f.input :id_historico_servicio
    = f.input :id_empleado
    = f.button :submit, class: 'btn btn-primary'
