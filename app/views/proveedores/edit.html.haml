= simple_form_for @proveedor, url:  proveedore_path, method: :put do |f|
    = f.input :id
    = f.input :id_sistema
    = f.input :sistema
    = f.input :id_empresa
    = f.input :nombre
    = f.button :submit, class: 'btn btn-primary'
