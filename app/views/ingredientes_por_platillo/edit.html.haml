= simple_form_for @ingrediente_por_platillo, url: ingredientes_por_platillo_path, method: :put do |f|
    = f.input :id, readonly: true
    = f.input :id_platillo, readonly: true
    = f.input :id_producto, readonly: true
    = f.input :id_tipo_medida, readonly: true
    - if valid_number?(@ingrediente_por_platillo.cantidad)
        = f.input :cantidad, readonly: true 
    - else 
        = f.input :cantidad, disabled: false
    = f.button :submit, class: 'btn btn-primary'
