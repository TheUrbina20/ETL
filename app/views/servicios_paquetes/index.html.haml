<h1>Servicio de paquetes</h1>
%table.table.table-striped
  %thead
    %tr
      %td='ID'
      %td='Num historico / servicio'
      %td='Num paquetes'
  %tbody
    - @serviciop.each do |s|
      %tr
        %td=s.id
        %td=s.id_historico_servicio
        %td=s.id_paquete
